import React from 'react';

const Lunch = (props) => {
    // const {id, title, description, img} = props.breakfast;
    // console.log(props.breakfast)
    return (
        <div>
            {/* <img src={img} alt="" /> */}
        </div>
    );
};

export default Lunch;